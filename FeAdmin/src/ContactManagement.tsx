import { useEffect, useState } from 'react'
import './contact.css'

type Contact = { MaLienHe: number; MaNguoiDung?: number; TenDangNhap?: string; HoTen: string; Email?: string; SoDienThoai?: string; ChuDe?: string; NoiDung: string; TrangThai: 'CHUA_XU_LY' | 'DANG_XU_LY' | 'DA_XU_LY'; NgayGui: string; NgayXuLy?: string }
const API = import.meta.env.VITE_API_URL || 'http://localhost:7000'
const headers = () => ({ 'Content-Type': 'application/json', ...(localStorage.getItem('admin_token') ? { Authorization: `Bearer ${localStorage.getItem('admin_token')}` } : {}) })
const statusNames: Record<Contact['TrangThai'], string> = { CHUA_XU_LY: 'ChÆ°a xá»­ lÃ½', DANG_XU_LY: 'Äang xá»­ lÃ½', DA_XU_LY: 'ÄÃ£ xá»­ lÃ½' }
const date = (value?: string) => value ? new Date(value).toLocaleString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' }) : '-'
const demo: Contact[] = []

export default function ContactManagement() { const [rows, setRows] = useState<Contact[]>(demo); const [query, setQuery] = useState(''); const [filter, setFilter] = useState('ALL'); const [selected, setSelected] = useState<Contact | null>(null); const [loading, setLoading] = useState(false); const load = async () => { setLoading(true); try { const response = await fetch(`${API}/api/lienhe`, { headers: headers() }); if (response.ok) { const body = await response.json(); if (Array.isArray(body.data)) setRows(body.data) } } catch { /* Demo data keeps the screen usable without the API. */ } finally { setLoading(false) } }; useEffect(() => { void load() }, []); const filtered = rows.filter((row) => (filter === 'ALL' || row.TrangThai === filter) && `${row.HoTen} ${row.Email || ''} ${row.SoDienThoai || ''} ${row.ChuDe || ''} ${row.NoiDung}`.toLowerCase().includes(query.toLowerCase())); const update = async (row: Contact, next: Contact['TrangThai']) => { try { const response = await fetch(`${API}/api/lienhe/${row.MaLienHe}/status`, { method: 'PUT', headers: headers(), body: JSON.stringify({ TrangThai: next }) }); if (!response.ok) throw new Error(); const updated = { ...row, TrangThai: next, NgayXuLy: next === 'DA_XU_LY' ? new Date().toISOString() : undefined }; setRows((current) => current.map((item) => item.MaLienHe === row.MaLienHe ? updated : item)); setSelected(selected?.MaLienHe === row.MaLienHe ? updated : selected) } catch { window.alert('KhÃ´ng thá»ƒ cáº­p nháº­t tráº¡ng thÃ¡i liÃªn há»‡.') } }; return <section className="contact-page"><div className="contact-heading"><div><small>LIÃŠN Há»† / YÃŠU Cáº¦U KHÃCH HÃ€NG</small><h1>Quáº£n lÃ½ liÃªn há»‡</h1><p>Tiáº¿p nháº­n, phÃ¢n loáº¡i vÃ  theo dÃµi tiáº¿n Ä‘á»™ xá»­ lÃ½ cÃ¡c yÃªu cáº§u gá»­i vá» FashionStore.</p></div><button className="outline-action">â‡© Xuáº¥t dá»¯ liá»‡u</button></div><div className="contact-metrics"><Metric title="Tá»•ng yÃªu cáº§u" value={rows.length} note="toÃ n há»‡ thá»‘ng" tone="blue" /><Metric title="ChÆ°a xá»­ lÃ½" value={rows.filter((row) => row.TrangThai === 'CHUA_XU_LY').length} note="cáº§n tiáº¿p nháº­n" tone="orange" /><Metric title="Äang xá»­ lÃ½" value={rows.filter((row) => row.TrangThai === 'DANG_XU_LY').length} note="Ä‘ang phá»¥ trÃ¡ch" tone="violet" /><Metric title="ÄÃ£ xá»­ lÃ½" value={rows.filter((row) => row.TrangThai === 'DA_XU_LY').length} note="hoÃ n táº¥t" tone="green" /></div><div className="contact-tabs">{[['ALL', 'Táº¥t cáº£'], ['CHUA_XU_LY', 'ChÆ°a xá»­ lÃ½'], ['DANG_XU_LY', 'Äang xá»­ lÃ½'], ['DA_XU_LY', 'ÄÃ£ xá»­ lÃ½']].map(([key, label]) => <button className={filter === key ? 'selected' : ''} key={key} onClick={() => setFilter(key)}>{label}<b>{key === 'ALL' ? rows.length : rows.filter((row) => row.TrangThai === key).length}</b></button>)}</div><div className="contact-layout"><div><div className="contact-filters"><label>âŒ• <input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="TÃ¬m tÃªn, email, chá»§ Ä‘á» hoáº·c ná»™i dung..." /></label><button>â–£ NgÃ y gá»­i â–¾</button><button>â‹® Bá»™ lá»c nÃ¢ng cao</button></div><div className="contact-table-card"><div className="table-caption"><strong>{filtered.length} yÃªu cáº§u liÃªn há»‡</strong><span>{loading ? 'Äang Ä‘á»“ng bá»™...' : 'Cáº­p nháº­t tá»« LienHe'}</span></div><table className="contact-table"><thead><tr><th>MÃ£</th><th>NgÆ°á»i gá»­i</th><th>Chá»§ Ä‘á»</th><th>Ná»™i dung</th><th>NgÃ y gá»­i</th><th>Tráº¡ng thÃ¡i</th><th>Thao tÃ¡c</th></tr></thead><tbody>{filtered.map((row) => <tr className={selected?.MaLienHe === row.MaLienHe ? 'active-row' : ''} key={row.MaLienHe}><td><b className="contact-code">LH-{String(row.MaLienHe).padStart(5, '0')}</b></td><td><strong>{row.HoTen}</strong><small>{row.Email || row.SoDienThoai || 'ChÆ°a cÃ³ liÃªn há»‡'}</small></td><td>{row.ChuDe || 'KhÃ´ng cÃ³ chá»§ Ä‘á»'}</td><td className="message-preview">{row.NoiDung}</td><td>{date(row.NgayGui)}</td><td><span className={`contact-status ${row.TrangThai}`}>{statusNames[row.TrangThai]}</span></td><td><button className="table-action" onClick={() => setSelected(row)}>Xem</button></td></tr>)}</tbody></table>{filtered.length === 0 && <div className="empty-state">KhÃ´ng cÃ³ yÃªu cáº§u phÃ¹ há»£p.</div>}</div></div>{selected && <ContactDetail contact={selected} onClose={() => setSelected(null)} onUpdate={update} />}</div></section> }

function ContactDetail({ contact, onClose, onUpdate }: { contact: Contact; onClose: () => void; onUpdate: (contact: Contact, next: Contact['TrangThai']) => Promise<void> }) { return <aside className="contact-detail"><div className="detail-title"><div><small>CHI TIáº¾T YÃŠU Cáº¦U</small><h2>LH-{String(contact.MaLienHe).padStart(5, '0')}</h2></div><button onClick={onClose}>Ã—</button></div><span className={`contact-status ${contact.TrangThai}`}>{statusNames[contact.TrangThai]}</span><div className="detail-section"><h3>ThÃ´ng tin ngÆ°á»i gá»­i</h3><strong>{contact.HoTen}</strong><p>{contact.Email || 'ChÆ°a cÃ³ email'}<br />{contact.SoDienThoai || 'ChÆ°a cÃ³ sá»‘ Ä‘iá»‡n thoáº¡i'}</p></div><div className="detail-section"><h3>Ná»™i dung liÃªn há»‡</h3><strong>{contact.ChuDe || 'KhÃ´ng cÃ³ chá»§ Ä‘á»'}</strong><p className="detail-message">{contact.NoiDung}</p></div><div className="detail-section"><h3>Thá»i gian</h3><p>NgÃ y gá»­i: {date(contact.NgayGui)}<br />NgÃ y xá»­ lÃ½: {date(contact.NgayXuLy)}</p></div><div className="detail-actions"><select value={contact.TrangThai} onChange={(event) => void onUpdate(contact, event.target.value as Contact['TrangThai'])}>{Object.entries(statusNames).map(([key, label]) => <option value={key} key={key}>{label}</option>)}</select><button className="primary-action" onClick={() => void onUpdate(contact, 'DA_XU_LY')}>ÄÃ¡nh dáº¥u Ä‘Ã£ xá»­ lÃ½</button></div></aside> }
function Metric({ title, value, note, tone }: { title: string; value: string | number; note: string; tone: string }) { return <article className={`contact-metric ${tone}`}><span>{title}</span><strong>{value}</strong><small>{note}</small></article> }