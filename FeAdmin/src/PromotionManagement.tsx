import { useEffect, useState } from 'react'
import './promotion.css'

type Voucher = { MaGiamGia: number; MaCode: string; TenMaGiamGia: string; MoTa?: string; LoaiGiam: 'PHAN_TRAM' | 'SO_TIEN'; GiaTriGiam: number; GiamToiDa?: number; DonToiThieu: number; TongSoLuong?: number; SoLuongDaSuDung: number; SoLanSuDungMoiNguoi: number; NgayBatDau: string; NgayKetThuc: string; TrangThai: number }
type Program = { MaChuongTrinh: number; TenChuongTrinh: string; MoTa?: string; LoaiGiam: 'PHAN_TRAM' | 'SO_TIEN'; GiaTriGiam: number; NgayBatDau: string; NgayKetThuc: string; TrangThai: number }
const API = import.meta.env.VITE_API_URL || 'http://localhost:7000'
const headers = () => ({ 'Content-Type': 'application/json', ...(localStorage.getItem('admin_token') ? { Authorization: `Bearer ${localStorage.getItem('admin_token')}` } : {}) })
const money = (value: number) => `${Number(value || 0).toLocaleString('vi-VN')} d`
const date = (value?: string) => value ? new Date(value).toLocaleDateString('vi-VN') : '-'
const demoVouchers: Voucher[] = []
const demoPrograms: Program[] = []

export default function PromotionManagement({ mode }: { mode: string }) { return mode === 'vouchers' ? <VoucherScreen /> : <ProgramScreen flash={mode === 'flash-sale'} /> }

function VoucherScreen() { const [rows, setRows] = useState<Voucher[]>(demoVouchers); const [query, setQuery] = useState(''); const [filter, setFilter] = useState('ALL'); const [form, setForm] = useState(false); const [loading, setLoading] = useState(false); const load = async () => { setLoading(true); try { const response = await fetch(`${API}/api/admin/vouchers`, { headers: headers() }); if (response.ok) { const body = await response.json(); if (Array.isArray(body.data)) setRows(body.data) } } catch { /* Demo data keeps the screen usable without the API. */ } finally { setLoading(false) } }; useEffect(() => { void load() }, []); const filtered = rows.filter((row) => (filter === 'ALL' || (filter === 'ACTIVE' ? row.TrangThai === 1 : row.TrangThai === 0)) && `${row.MaCode} ${row.TenMaGiamGia}`.toLowerCase().includes(query.toLowerCase())); return <section className="promotion-page"><Header title="M� gi?m gi�" eyebrow="KHUY?N M�I / M� GI?M GI�" description="Qu?n l� c�c chuong tr�nh uu d�i, m� voucher khuy?n m�i, gi?i h?n ng�n s�ch v� di?u ki?n �p d?ng to�n h? th?ng." action="+ T?o m� gi?m gi�" onAction={() => setForm(true)} /><Metrics vouchers={rows} /><Tabs filter={filter} setFilter={setFilter} labels={['T?t c?', '�ang ch?y', '�� t?t']} counts={[rows.length, rows.filter((row) => row.TrangThai === 1).length, rows.filter((row) => row.TrangThai === 0).length]} /><div className="promotion-filters"><label>? <input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="T�m theo m� voucher, t�n chuong tr�nh..." /></label><button>Lo?i gi?m gi�: T?t c? ?</button><button>K�nh �p d?ng: To�n b? k�nh ?</button><button>? Th�ng hi?n t?i ?</button></div><div className="promo-table-card"><Caption count={filtered.length} loading={loading} source="MaGiamGia" /><table className="promo-table"><thead><tr><th>M� voucher</th><th>Lo?i gi?m</th><th>�i?u ki?n</th><th>�� s? d?ng</th><th>Th?i gian hi?u l?c</th><th>Tr?ng th�i</th><th>Thao t�c</th></tr></thead><tbody>{filtered.map((row) => <tr key={row.MaGiamGia}><td><strong className="code">{row.MaCode}</strong><small>{row.TenMaGiamGia}</small></td><td><strong>{row.LoaiGiam === 'PHAN_TRAM' ? `${row.GiaTriGiam}%` : money(row.GiaTriGiam)}</strong><small>T?i da: {row.GiamToiDa ? money(row.GiamToiDa) : 'Kh�ng gi?i h?n'}</small></td><td><strong>T? {money(row.DonToiThieu)}</strong><small>Th�nh vi�n: t?t c?</small></td><td><strong>{row.SoLuongDaSuDung.toLocaleString('vi-VN')} / {row.TongSoLuong || '8'}</strong><div className="usage"><i style={{ width: `${Math.min(100, row.TongSoLuong ? row.SoLuongDaSuDung / row.TongSoLuong * 100 : 0)}%` }} /></div></td><td>{date(row.NgayBatDau)} - {date(row.NgayKetThuc)}</td><td><span className={`promo-status ${row.TrangThai ? 'on' : 'off'}`}>{row.TrangThai ? '? �ang ch?y' : '? �� t?t'}</span></td><td><button className="table-action">?</button></td></tr>)}</tbody></table>{filtered.length === 0 && <div className="empty-state">Kh�ng c� m� gi?m gi� ph� h?p.</div>}</div>{form && <VoucherForm onClose={() => setForm(false)} onSaved={(row) => { setRows((current) => [row, ...current]); setForm(false) }} />}</section> }

function ProgramScreen({ flash }: { flash: boolean }) { const [rows, setRows] = useState<Program[]>(demoPrograms); const [query, setQuery] = useState(''); const [filter, setFilter] = useState('ALL'); const [form, setForm] = useState(false); const [loading, setLoading] = useState(false); useEffect(() => { setLoading(true); fetch(`${API}/api/admin/discount-programs`, { headers: headers() }).then((response) => response.ok ? response.json() : null).then((body) => { if (Array.isArray(body?.data)) setRows(body.data) }).catch(() => undefined).finally(() => setLoading(false)) }, []); const filtered = rows.filter((row) => (filter === 'ALL' || (filter === 'ACTIVE' ? row.TrangThai === 1 : row.TrangThai === 0)) && (!flash || row.TenChuongTrinh.toLowerCase().includes('flash')) && row.TenChuongTrinh.toLowerCase().includes(query.toLowerCase())); const title = flash ? 'Flash Sale' : 'Chuong tr�nh gi?m gi�'; return <section className="promotion-page"><Header title={title} eyebrow={`KHUY?N M�I / ${title.toUpperCase()}`} description="Theo d�i chuong tr�nh gi?m tr?c ti?p tr�n s?n ph?m ho?c bi?n th? theo th?i gian v� tr?ng th�i." action="+ T?o chuong tr�nh" onAction={() => setForm(true)} /><Metrics programs={rows} /><Tabs filter={filter} setFilter={setFilter} labels={['T?t c?', '�ang di?n ra', '�� k?t th�c']} counts={[rows.length, rows.filter((row) => row.TrangThai === 1).length, rows.filter((row) => row.TrangThai === 0).length]} /><div className="promotion-filters"><label>? <input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="T�m theo t�n chuong tr�nh..." /></label><button>Lo?i gi?m gi�: T?t c? ?</button><button>? Th?i gian hi?u l?c ?</button></div><div className="promo-table-card"><Caption count={filtered.length} loading={loading} source="ChuongTrinhGiamGia" /><table className="promo-table"><thead><tr><th>Chuong tr�nh</th><th>M?c gi?m</th><th>Th?i gian hi?u l?c</th><th>S?n ph?m �p d?ng</th><th>Tr?ng th�i</th><th>Thao t�c</th></tr></thead><tbody>{filtered.map((row) => <tr key={row.MaChuongTrinh}><td><strong>{row.TenChuongTrinh}</strong><small>{row.MoTa || 'Chua c� m� t?'}</small></td><td><strong>{row.LoaiGiam === 'PHAN_TRAM' ? `${row.GiaTriGiam}%` : money(row.GiaTriGiam)}</strong><small>{row.LoaiGiam === 'PHAN_TRAM' ? 'Theo ph?n tram' : 'Theo s? ti?n'}</small></td><td>{date(row.NgayBatDau)} - {date(row.NgayKetThuc)}</td><td><span className="scope-pill">T?t c? s?n ph?m</span><small>Chua g?n s?n ph?m</small></td><td><span className={`promo-status ${row.TrangThai ? 'on' : 'off'}`}>{row.TrangThai ? '? �ang ch?y' : '? �� t?t'}</span></td><td><button className="table-action">?</button></td></tr>)}</tbody></table>{filtered.length === 0 && <div className="empty-state">Chua c� chuong tr�nh ph� h?p. Flash Sale c?n t�n chuong tr�nh c� t? �Flash�.</div>}</div>{form && <ProgramForm onClose={() => setForm(false)} onSaved={(row) => { setRows((current) => [row, ...current]); setForm(false) }} />}</section> }

function VoucherForm({ onClose, onSaved }: { onClose: () => void; onSaved: (row: Voucher) => void }) { const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const values = Object.fromEntries(new FormData(event.currentTarget).entries()); const payload = { ...values, GiaTriGiam: Number(values.GiaTriGiam), GiamToiDa: Number(values.GiamToiDa) || null, DonToiThieu: Number(values.DonToiThieu), TongSoLuong: Number(values.TongSoLuong) || null, SoLanSuDungMoiNguoi: Number(values.SoLanSuDungMoiNguoi), TrangThai: 1 }; try { const response = await fetch(`${API}/api/admin/vouchers`, { method: 'POST', headers: headers(), body: JSON.stringify(payload) }); if (!response.ok) throw new Error(); const body = await response.json(); onSaved({ ...payload, MaGiamGia: body.insertId, SoLuongDaSuDung: 0 } as Voucher) } catch { window.alert('Kh�ng th? t?o m� gi?m gi�. Ki?m tra ng�y v� d? li?u b?t bu?c.') } }; return <Modal title="T?o m� gi?m gi�" onClose={onClose}><form onSubmit={submit}><div className="form-grid"><label>M� code<input name="MaCode" required placeholder="FASHION20" /></label><label>T�n m�<input name="TenMaGiamGia" required placeholder="Uu d�i th�ng 9" /></label><label>Lo?i gi?m<select name="LoaiGiam"><option value="PHAN_TRAM">Ph?n tram</option><option value="SO_TIEN">S? ti?n</option></select></label><label>Gi� tr? gi?m<input name="GiaTriGiam" type="number" min="1" required /></label><label>Gi?m t?i da<input name="GiamToiDa" type="number" min="0" /></label><label>�on t?i thi?u<input name="DonToiThieu" type="number" min="0" defaultValue="0" /></label><label>T?ng s? lu?ng<input name="TongSoLuong" type="number" min="1" /></label><label>Lu?t/ngu?i<input name="SoLanSuDungMoiNguoi" type="number" min="1" defaultValue="1" /></label><label>B?t d?u<input name="NgayBatDau" type="datetime-local" required /></label><label>K?t th�c<input name="NgayKetThuc" type="datetime-local" required /></label></div><label>M� t?<textarea name="MoTa" /></label><button className="primary-action">Luu m� gi?m gi�</button></form></Modal> }
function ProgramForm({ onClose, onSaved }: { onClose: () => void; onSaved: (row: Program) => void }) { const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const values = Object.fromEntries(new FormData(event.currentTarget).entries()); const payload = { ...values, GiaTriGiam: Number(values.GiaTriGiam), TrangThai: 1 }; try { const response = await fetch(`${API}/api/admin/discount-programs`, { method: 'POST', headers: headers(), body: JSON.stringify(payload) }); if (!response.ok) throw new Error(); const body = await response.json(); onSaved({ ...payload, MaChuongTrinh: body.insertId } as Program) } catch { window.alert('Kh�ng th? t?o chuong tr�nh. Ki?m tra ng�y v� d? li?u b?t bu?c.') } }; return <Modal title="T?o chuong tr�nh gi?m gi�" onClose={onClose}><form onSubmit={submit}><label>T�n chuong tr�nh<input name="TenChuongTrinh" required placeholder="Flash Sale cu?i tu?n" /></label><div className="form-grid"><label>Lo?i gi?m<select name="LoaiGiam"><option value="PHAN_TRAM">Ph?n tram</option><option value="SO_TIEN">S? ti?n</option></select></label><label>Gi� tr? gi?m<input name="GiaTriGiam" type="number" min="1" required /></label><label>B?t d?u<input name="NgayBatDau" type="datetime-local" required /></label><label>K?t th�c<input name="NgayKetThuc" type="datetime-local" required /></label></div><label>M� t?<textarea name="MoTa" /></label><button className="primary-action">Luu chuong tr�nh</button></form></Modal> }
function Header({ eyebrow, title, description, action, onAction }: { eyebrow: string; title: string; description: string; action: string; onAction: () => void }) { return <div className="promotion-heading"><div><small>{eyebrow}</small><h1>{title}</h1><p>{description}</p></div><div><button className="outline-action">? Xu?t Excel</button><button className="primary-action" onClick={onAction}>{action}</button></div></div> }
function Metrics({ vouchers, programs }: { vouchers?: Voucher[]; programs?: Program[] }) { const total = vouchers || programs || []; const active = total.filter((row) => row.TrangThai === 1).length; return <div className="promotion-metrics"><Metric title={vouchers ? 'T?ng m� dang ch?y' : 'T?ng chuong tr�nh'} value={active} note="dang ho?t d?ng" tone="violet" /><Metric title="Lu?t s? d?ng" value={vouchers ? vouchers.reduce((sum, row) => sum + row.SoLuongDaSuDung, 0).toLocaleString('vi-VN') : 'Theo d�i'} note="t? h? th?ng" tone="blue" /><Metric title="Gi?m gi� dang �p d?ng" value={vouchers ? money(vouchers.reduce((sum, row) => sum + row.GiaTriGiam, 0)) : `${active} chuong tr�nh`} note="d? li?u hi?n t?i" tone="green" /><Metric title="S?p k?t th�c" value={total.filter((row) => new Date(row.NgayKetThuc) > new Date() && new Date(row.NgayKetThuc).getTime() - Date.now() < 7 * 86400000).length} note="trong 7 ng�y t?i" tone="orange" /></div> }
function Metric({ title, value, note, tone }: { title: string; value: string | number; note: string; tone: string }) { return <article className={`promotion-metric ${tone}`}><span>{title}</span><strong>{value}</strong><small>{note}</small></article> }
function Tabs({ filter, setFilter, labels, counts }: { filter: string; setFilter: (value: string) => void; labels: string[]; counts: number[] }) { return <div className="promotion-tabs">{labels.map((label, index) => { const key = index === 0 ? 'ALL' : index === 1 ? 'ACTIVE' : 'INACTIVE'; return <button className={filter === key ? 'selected' : ''} key={key} onClick={() => setFilter(key)}>{label}<b>{counts[index]}</b></button> })}</div> }
function Caption({ count, loading, source }: { count: number; loading: boolean; source: string }) { return <div className="table-caption"><strong>{count} b?n ghi</strong><span>{loading ? '�ang d?ng b?...' : `C?p nh?t t? ${source}`}</span></div> }
function Modal({ title, onClose, children }: { title: string; onClose: () => void; children: React.ReactNode }) { return <div className="modal-backdrop"><div className="promo-modal"><button className="modal-close" onClick={onClose}>�</button><h2>{title}</h2><p>Nh?p d? li?u theo d�ng schema khuy?n m�i.</p>{children}</div></div> }